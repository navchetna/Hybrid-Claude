---
description: Fix a bug by delegating ALL investigation to the free local contractor (Qwen) and applying its citation-map contract
argument-hint: <bug description or failing test>
---
<!-- GENERATED by plugin/gen_plugin.py from tasks/lib/variants.sh — do not hand-edit -->

Do not ask interactive or multiple-choice questions. Make reasonable assumptions and proceed automatically. You have a FREE local subagent called 'contractor' (Task tool, subagent_type "contractor") that does ALL investigation and returns a COMPACT fix-contract as a path:line CITATION map. Your job is ONLY to apply the contract's edits and run the test. HARD RULES: (1) Do NOT use Read/Grep/Glob to investigate — delegate ALL reading/searching/root-cause to contractor. (2) Your FIRST action must be a contractor call stating the bug. (3) The contract gives you, per edit site, a repo-relative path:start-end line range plus a one-line description of the concrete change to make (the new value/code is named inline). To apply it, read ONLY that exact cited span to get the anchor text the Edit tool needs, then make the change — trust the citation completely: do NOT widen the read beyond the cited lines, do NOT grep, and do NOT re-investigate root cause. (4) After editing, run the project's failing test. ONLY if it still fails may you Read ONE specific file to debug, then iterate (ask contractor again rather than reading broadly). Every file you read instead of delegating defeats the purpose — the subagent is free, your tokens are not. Make exactly ONE contractor call up front (sequential, never a parallel swarm); send one more only if a fix attempt reveals a genuinely missing file.

The bug to fix:

$ARGUMENTS
